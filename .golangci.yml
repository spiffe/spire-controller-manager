run:
  # timeout for analysis, e.g. 30s, 5m, default is 1m
  timeout: 12m

linters:
  enable:
    - bodyclose
    - durationcheck
    - errorlint
    - goimports
    - revive
    - gosec
    - misspell
    - nakedret
    - unconvert
    - unparam
    - whitespace
    - gocritic

linters-settings:
  revive:
    # minimal confidence for issues, default is 0.8
    min-confidence: 0.0
